import { WalletNumberPipe } from './wallet-number-pipe';

describe('WalletNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
